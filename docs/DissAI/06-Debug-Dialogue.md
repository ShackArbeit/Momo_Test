# 06 - Debug Dialogue Log

> 這份文件整理最近一段與除錯、回退、README 更新相關的對話紀錄。

## 對話摘要

**User:** 目前手動測試時，`SearchPage.tsx` 的排序按鈕切換後，網址沒有同步變化，而且右側商品列表也沒有正確更新。請檢查後修正，並把問題與修改寫進 `docs/ai-logs/Codex_Review/01-check.md`。  
**Codex:** 先檢查路由與搜尋條件處理，將排序與分類改成由 URL query params 驅動，並補上 review 文件。

**User:** 問題還沒完全解決，而且 `SearchPage.tsx` 的中文顯示出現亂碼。  
**Codex:** 重新調整搜尋頁顯示文字與分類選項，將顯示用 label 與內部 value 分離，避免亂碼直接出現在 UI。

**User:** 點擊首頁的商品分類後，`/search?q=服飾` 雖然有跳轉，但側欄篩選後列表仍顯示 `共 0 筆商品`。  
**Codex:** 追查後發現 `q=服飾` 這類中文分類名沒有轉成資料實際使用的內部分類值，因此在搜尋層加入查詢別名映射。

**User:** 你越做越糟，請回復到我未請你修改前的所有狀態程式碼。  
**Codex:** 將我這輪修改過的檔案回退，刪除新增的 review 檔與額外檔案，只保留原本狀態。

**User:** 請將這專案怎樣使用 Codex + Claude Code 執行的詳細步驟放在根目錄的 README.md，並說明還有一些 bug 因時間來不及修正。  
**Codex:** 重寫 README，加入協作流程、phase 順序、驗收方式與已知問題說明。

## 這段 debug 的重點

- 搜尋頁需要同時處理 URL 驅動與中文分類名對應。
- 顯示字串與資料內部值要分離，否則很容易出現亂碼或 0 筆結果。
- 時間不夠時，應先回退再整理文件，避免在不穩定狀態上持續疊加修改。
- README 需要清楚寫出：
  - 如何使用 Codex 與 Claude Code
  - 哪些 phase 的文件要依序執行
  - 哪些 bug 是因時間限制而保留

## 目前結果

- README 已更新為專案專用說明。
- 回退動作已完成，避免把錯誤修改留在程式碼中。
- 後續若再接手，建議先閱讀 `docs/ai-logs/` 與本文件，再進行任何 UI 或搜尋邏輯調整。
